// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyA0W6aD8KyF6f2Qhl38o4tbt6w0mT3zpRs',
    authDomain: 'chat-app-b716e.firebaseapp.com',
    databaseURL: 'https://chat-app-b716e.firebaseio.com',
    projectId: 'chat-app-b716e',
    storageBucket: 'chat-app-b716e.appspot.com',
    messagingSenderId: '50506379446',
    appId: '1:50506379446:web:b068513bb011962c053617'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
